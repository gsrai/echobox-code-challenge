{
  "name": "twitter_char_count",
  "version": "1.0.0",
  "description": "So looking at the Twitter Docs, character count is a bit more complicated than a simple `'some tweet'.length`. Twitter uses the Unicode character set with UTF-8 encoding. It counts the codepoints of a NFC normalised string. NFC stands for Normalisation form C. However even then its not that simple.",
  "main": "index.js",
  "scripts": {
    "test": "jest"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "jest": "^23.6.0"
  }
}
